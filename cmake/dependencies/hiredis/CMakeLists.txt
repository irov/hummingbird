CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
                                             
project(hiredis)

SET(PROJECT_SOURCE_DIRECTORY ${HUMMINGBIRD_DEPENDENCIES_SOURCE_DIRECTORY}/hiredis)

# OUR SOURCES
set(PROJECT_SOURCES
    ${PROJECT_SOURCE_DIRECTORY}/async.c
    ${PROJECT_SOURCE_DIRECTORY}/dict.c
    ${PROJECT_SOURCE_DIRECTORY}/hiredis.c
    ${PROJECT_SOURCE_DIRECTORY}/net.c
    ${PROJECT_SOURCE_DIRECTORY}/read.c
    ${PROJECT_SOURCE_DIRECTORY}/sds.c
    ${PROJECT_SOURCE_DIRECTORY}/read.c    
)

include_directories(${PROJECT_SOURCE_DIRECTORY})

add_library(${PROJECT_NAME} STATIC ${PROJECT_SOURCES})
