PROJECT(hb_node_newuser C)

ADD_FILTER(
src
	hb_node_newuser.h
	hb_node_newuser.c
)

ADD_EXECUTABLE(${PROJECT_NAME} WIN32 ${HUMMINGBIRD_SOURCE_FILES})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} hb_node_main)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} hb_token)
